<%= form_with(model: archive) do |form| %>
  <% if archive.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(archive.errors.count, "error") %> prohibited this app_archive from being saved:</h2>

      <ul>
        <% archive.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
