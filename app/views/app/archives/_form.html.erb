<%= form_with(model: app_archive) do |form| %>
  <% if app_archive.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(app_archive.errors.count, "error") %> prohibited this app_archive from being saved:</h2>

      <ul>
        <% app_archive.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
