<%#
  This template partial is based on app/views/file_systems/_file_system.html.erb so any changes should be made there too
  and vise versa
%>
<section class="file-system">
  <div class="bread-crumbs">
    <% directory.file_system_route.each do |breadcrumb| %>
      <% if breadcrumb.is_a? FileSystem %>
        <%= link_to file_system_path(breadcrumb) do %>
          <i class="fa-solid fa-folder-tree"></i>
        <% end %>
      <% elsif breadcrumb == directory %>
        <span><%= breadcrumb.name %></span>
      <% else %>
        <%= link_to breadcrumb.name, file_system_directory_path(file_system, breadcrumb) %>
      <% end %>
      <span>/</span>
    <% end %>
  </div>
  <div class="file-system-items" data-controller="dialog">
    <% directory.directories.each do |directory| %>
      <%= link_to file_system_directory_path(file_system, directory), class: "file-system-item" do %>
        <i class="fa-solid fa-folder"></i> <span><%= directory.name %></span>
      <% end %>
    <% end %>
    <% directory.files.each do |file| %>
      <%= link_to download_file_system_file_path(file_system, file), class: "file-system-item", target: "_blank", data: { action: "click->dialog#open", dialog: dom_id(file) } do %>
        <i class="fa-regular fa-file"></i> <span><%= file.name %></span>
      <% end %>
      <dialog class="file-previewer" name="<%= dom_id(file) %>">
        <div class="header" style="display: flex; justify-content: space-between; align-items: center; gap: 0.5rem;">
          <h4 style="flex: 1; text-align: center">File Details</h4>
          <button data-action="click->dialog#close">
            <i class="fa-solid fa-xmark lg-xs"></i>
          </button>
        </div>
        <div class="body" style="display: flex; gap: 1rem;">
          <% if file.file.image? %>
            <%= image_tag attachment_file_system_file_path(file_system, file), width: 600, height: 600, alt: file.name %>
          <% end %>
          <div>
            <p>Name: <%= file.name %></p>
            <p>Size: <%= number_to_human_size(file.file.byte_size) %></p>
            <% if file.versions.any? %>
              <h6>Versions</h6>
              <% file.versions.each do |version| %>
                <%= link_to download_file_system_file_path(file_system, version), target: "_blank" do %>
                  <p><%= version.version %>: <%= number_to_human_size(version.file.byte_size) %></p>
                <% end %>
              <% end %>
            <% end %>
          </div>
        </div>
      </dialog>
    <% end %>
  </div>
</section>